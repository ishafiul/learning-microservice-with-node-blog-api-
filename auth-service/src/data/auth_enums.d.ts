export as namespace enums;

export const enum UserType {
    ADMIN = "ADMIN",
    USER = "USER",
}

export const enum HttpStatus {
    SUCCESS = "SUCCESS",
    CREATED = "CREATED",
    ERROR = "ERROR",
}
